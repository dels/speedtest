{
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "dotenv": "^10.0.0",
    "meow": "^10.1.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^8.5.0",
    "jest": "^27.4.5"
  },
  "scripts": {
    "dev": "cross-env NODE_ENV=test NODE_OPTIONS=--experimental-vm-modules jest --watch",
    "lint": "eslint .",
    "lint:fix": "eslint --fix --ext .js, ."
  }
}
